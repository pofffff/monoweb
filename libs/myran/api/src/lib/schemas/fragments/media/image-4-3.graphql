# Hero image mobile
fragment image43 on FileField {
  ...imageBase
  responsiveImage(
    locale: en
    imgixParams: { fit: crop, crop: focalpoint, ar: "4:3", fm: jpg, q: 100 }
  ) {
    ...responsiveImage
  }
}
